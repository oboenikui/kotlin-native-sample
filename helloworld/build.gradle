apply plugin: 'konan'

konanArtifacts {

    interop('stdio') {
        defFile "src/main/c_interop/stdio.def"
    }

    program('HelloWorld') {
        libraries {
            artifact 'stdio'
        }
    }
}