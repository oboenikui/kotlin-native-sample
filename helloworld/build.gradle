apply plugin: 'konan'

konanInterop {
    stdio {
    }
}

konanArtifacts {
    HelloWorld {
        useInterop 'stdio'
    }
}

task cleanKonan(type: Delete) {
    println("${projectDir}/build/konan/bin/*")
    delete fileTree("build/konan/bin") {
        include '*'
    }
}